# Build output — regenerated by CI on every push, never committed
book/_build/

# Local Claude Code config / preview launch settings
.claude/

# Scratch: extracted notebook text used while authoring the book
_extract/

# Original workshop source material — kept locally, NOT published.
# These folders hold the raw Chinese notebooks and the two rasters that exceed
# GitHub's 100 MB file limit (NE2_50M_SR_W.tif 167 MB, species_richness_1km 150 MB).
# Those two are slated for external (Zenodo) hosting via fetch_data.py.
20250714-上午/
20250714-下午/
20250715-上午/
20250715-下午/
20250716/
20250716-上午/
Class*.pptx

# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
.venv/
venv/
.DS_Store
